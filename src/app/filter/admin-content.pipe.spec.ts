import { AdminContentPipe } from './admin-content.pipe';

describe('AdminContentPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminContentPipe();
    expect(pipe).toBeTruthy();
  });
});
